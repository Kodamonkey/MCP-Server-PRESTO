{
  "mcpServers": {
    "presto": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "REPLACE_WITH_REPO_ROOT",
        "python",
        "-m",
        "presto_mcp.server"
      ]
    }
  }
}
